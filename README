Run:
./scripts/install.sh  # i.e. the first time you install all the things, or to recompile YCM

or:
./scripts/sync.sh  # this moves .vimrc* into your $HOME dir, removes the unused plugins, and installs the new ones.
